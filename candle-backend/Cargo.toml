[package]
name = "candle-rocm"
version = "0.1.0"
edition = "2021"
description = "ROCm/HIP backend for candle ML framework"
license = "MIT OR Apache-2.0"
repository = "https://github.com/Nu11ified/candle-rocm"

[dependencies]
candle-core = { package = "candle-core-mold", path = "../candle-core", features = ["rocm"] }
hip-runtime = { path = "../hip-runtime" }
